---
interface Props { hints: string[] }
const { hints } = Astro.props;
import ExerciseDisclosure from './ExerciseDisclosure.astro';
---
{hints.length > 0 && (
  <ExerciseDisclosure summary="Hints">
    <ol>{hints.map((hint) => <li>{hint}</li>)}</ol>
  </ExerciseDisclosure>
)}
