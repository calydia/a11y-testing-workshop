---
interface Finding { title: string; explanation: string }
interface Props { solution?: { summary: string; findings: Finding[] } }
const { solution } = Astro.props;
import ExerciseDisclosure from './ExerciseDisclosure.astro';
---
{solution && (
  <ExerciseDisclosure summary="Solution">
    <p>{solution.summary}</p>
    {solution.findings.map((finding) => (
      <section>
        <h3>{finding.title}</h3>
        <p>{finding.explanation}</p>
      </section>
    ))}
  </ExerciseDisclosure>
)}
