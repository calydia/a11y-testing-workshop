---
interface Props { items: string[] }
const { items } = Astro.props;
---
{items.length > 0 && (
  <section aria-labelledby="interpretation-title">
    <h2 id="interpretation-title">Interpreting the results</h2>
    <ul>{items.map((item) => <li>{item}</li>)}</ul>
  </section>
)}
